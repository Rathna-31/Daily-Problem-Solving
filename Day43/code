class Solution:
    def lengthOfLastWord(self, s: str) -> int:

        word = s.split(' ')
        print(word)

        for i in range(len(word)-1,-1,-1):
            if(word[i] != ''):
                return len(word[i])


        
